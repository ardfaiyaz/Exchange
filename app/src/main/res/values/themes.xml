<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Exchange" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Exchange" parent="Base.Theme.Exchange" />

    <style name="accinputerror">
        <item name="stTextColor">#FFFFFF</item>
        <item name="stColorBackground">#3a3a46</item>
        <item name="stIconStart">@drawable/erroricontoast</item>
        <item name="stRadius">10dp</item>
        <item name="stFont">@font/montserrat_bold</item>
        <item name="stSolidBackground">true</item>
        <item name="stLength">LONG</item>
        <item name="elevation">19dp</item>
    </style>

    <style name="placedordertoast">
        <item name="stTextColor">#FFFFFF</item>
        <item name="stColorBackground">#3a3a46</item>
        <item name="stIconStart">@drawable/checkicontoast</item>
        <item name="stRadius">10dp</item>
        <item name="stFont">@font/montserrat_bold</item>
        <item name="stSolidBackground">true</item>
        <item name="stLength">LONG</item>
        <item name="elevation">19dp</item>
    </style>
</resources>